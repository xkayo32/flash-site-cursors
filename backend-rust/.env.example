# Database Configuration
DATABASE_URL=postgresql://estudos_user:estudos_pass@localhost:5532/estudos_db

# Server Configuration
PORT=8180
HOST=0.0.0.0
RUST_ENV=development
RUST_LOG=info

# Security
JWT_SECRET=your-jwt-secret-change-in-production

# CORS Configuration (comma-separated list)
CORS_ORIGINS=http://localhost:5173,http://localhost:5273,http://localhost:3000

# Database Pool Settings (optional, will use defaults if not set)
DB_MAX_CONNECTIONS=10
DB_MIN_CONNECTIONS=1
DB_ACQUIRE_TIMEOUT=30
DB_IDLE_TIMEOUT=600